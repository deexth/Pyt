"""Same as chapter 8, except importing module into modules
for example if from the program with the Car class we needed to used Electric_Car class and they are in
separate files,
we have to first import the Car class module then import Electric_Car from its module."""