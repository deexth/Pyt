#we will be checking how the if not works
requested_toppings = 'mushrooms'

if requested_toppings != 'anchovies':
    print("Hold the anchovies!")

    """here in this condition we have used the if not so that when the requested topping is not anchovies it
    prints a message. You can always use also numbers instead of a string for example checking the age."""
